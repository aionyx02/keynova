---
type: session_log
status: archive
priority: p3
updated: 2026-05-20
context_policy: on_demand
owner: project
---

# Session 2026-05-20

## Highlights

- Installed docs governance v1 based on `Keynova_Docs_Governance_Plan_v1.docx`.
- Reworked startup Markdown into compact current-state files.
- Added guard scripts for size, schema, frontmatter policy, narrative detection, session creation, completed-index generation, and session archiving.

## Detailed Notes

- `current.md` and `active.md` now contain only current state.
- `backlog.md` and `completed.md` are no longer startup context.
- `testing-edge-cases.md` was converted from incident-style narrative into reusable debug matrices.
- Detailed historical context stays in session logs and git history.
- REF.0: ADR-0029 (AI capability layer) and derived ADR-0030 (minimal risk tag contract) accepted by developer in conversation; AI executed status flip on explicit developer instruction (governance §4 conflict resolution: explicit developer instruction > §3 default rule).
- ADR slot reassignment: 0029 taken from AGENT.3 reservation; 0030 taken from AGENT.4 reservation. Both source tracks remain superseded/parked.
- ADR-0030 went through one pruning round: initial draft proposed 7-category × 4-severity schema; developer pushback on inline AI complexity led to minimal `{requires_confirmation, reason}` v1 contract with new-ADR extension rule for future category × severity needs.
- Resolutions captured in ADR-0029 §9 and ADR-0030 §9: single-shot chaining (UI-driven OK), `context_hash` optional, observation window = max(minor release tag, 14 calendar days) with no P0 regression, `core/agent_audit.rs` scoped to legacy approval audit only, in-flight chip disabled (state-based, no telemetry), `reason` 256 UTF-8 bytes char-boundary truncate.

## Commits

- Pending.

## Follow-ups

- Watch first few commits for guard thresholds that are too strict.
- REF.1 (UnifiedResult schema) is the immediate next batch.
- Pre-existing test failure observed during REF.1 verification: `handlers::builtin_cmd::tests::note_lazyvim_missing_nvim_returns_inline_guidance` (fails on Windows even with stashed working tree at HEAD `05d72e3`). Not caused by REF.1; flag for separate triage — likely PATH-based nvim detection bypassing the fake finder closure in the test.

## COMPLETED: DOCS.2 - Documentation governance v1 workflow and guard automation.

## COMPLETED: REF.0 - ADR-0029 + ADR-0030 accepted, REF.1+ unlocked.

## COMPLETED: REF.1 - UnifiedResult/ResultSource/ActionChip/ConfirmRequirement/PreviewPayload/RankSignals types + 3 conversion shims (SearchResult / BuiltinCommandResult / UiSearchItem) + TS twin; 9 unit tests pass; clippy -D warnings green.

## COMPLETED: REF.2.P3 - 6/6 ancillary hooks extracted

- useCopyHint (copiedPath + copyHint state with 1200ms TTL helpers): 8 tests covering flash supersede + clear + custom durations.
- useSearchBackend (search.backend IPC + config-reloaded listener): 1 unit-test for non-Tauri short-circuit.
- useLauncherSettings (launcher.max_results / search.preview_enabled / search.show_rank_breakdown + config-reloaded listener): 2 unit-tests for default state + non-Tauri short-circuit.
- useWorkspaceLifecycle (window-focused + workspace-switched + workspace-cycled listeners with shared reset chain): 1 unit-test; added **TAURI_INTERNALS** guard to window-focused listener.
- useSecondaryMenu (5 state pieces + closeSecondaryMenu reset): 3 tests covering reset semantics + expandedMetadata isolation.
- useEscapeKey (global ESC priority chain — secondary menu / metadata / terminal / cmdResult / query / hideWindow): 9 tests covering each priority branch + shouldIgnoreEscape gate + unmount cleanup.
- CommandPalette.tsx 1582 → 1304 lines (P2+P3 cumulative -278). Remaining reduction comes from P4 (JSX subcomponents) + P5 reassembly to hit <250 target.
- Smoke test file dropped now that 9 real hook test files exist.

## COMPLETED: REF.2.P1 + REF.2.P2 - feature folder scaffold + 3/3 priority hooks

- REF.2.P1: relocated CommandSuggestions / FilterChips / SecondaryActionMenu under src/features/command-palette/.
- Vitest + jsdom + @testing-library/react infra added (vitest.config.ts, vitest.setup.ts, npm run test / test:watch); verify pipeline now includes the frontend suite.
- REF.2.P2 useRecentlyDeleted (Bug B kill-set + 30s TTL) extracted with 6 focused tests.
- REF.2.P2 usePipeline (automation.execute_pipeline state) extracted with 5 focused tests.
- REF.2.P2 useSearchStream (Bug A chunk listener + 200ms debounce + activeRequestRef stale-result guard) extracted with 9 focused tests covering debounce timing, supersede behavior, cancelSearch, setSearchLimit clamping, success path sort/trim, and dispatch rejection branch.
- CommandPalette.tsx 1582 → 1455 lines (-127). REF.2 target is <250; remaining reduction comes from P3 (other hooks) and P4 (subcomponents).
