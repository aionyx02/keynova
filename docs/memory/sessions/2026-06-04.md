---
type: session_log
status: active
priority: p1
context_policy: on_demand
owner: project
updated: 2026-06-04
---

# Session 2026-06-04

## Highlights

- Landed MEM.1.B (local-model personalization + prompt budget) at unit level on
  `feature/personal-memory`. Backend-only; no frontend changes.

## COMPLETED: MEM.1.B - local-model personalization + prompt budget

Scope and decisions live in `docs/tasks/personal-memory.md` and ADR-0043 (proposed).
Privacy boundary: personal memory is injected into capability prompts only when the
resolved provider is local (ollama); cloud providers inject nothing.

Backend:

- `core/ai_capability/memory.rs` (new): `push_memory_sources(store, query, sources)`
  reads `scope="personal"` rows (`agent_memories_blocking`), term-ranks (title
  weight 2, content 1), and pushes up to 3 as `GroundingSource`s via
  `visibility_filtered_source` (secret-shaped content still redacted as defence in
  depth). Best-effort: store errors are swallowed so grounding never blocks a
  capability. `term_score` promoted here and reused by `recall.rs` (dropped its
  private `score_match` duplicate).
- `contract.rs`: `CapabilityDeps.allow_memory_grounding` flag (privacy gate).
- `handlers/ai_capability.rs`: sets the flag from
  `matches!(runtime.provider, AiProvider::Ollama { .. })`, computed before
  `runtime` is moved into the chat provider.
- `explain.rs` / `fix_error.rs` / `gen_command.rs`: call `push_memory_sources`
  only when `allow_memory_grounding` is set, keyed on the user query / raw error /
  intent respectively.
- `prompt.rs`: over-budget fallback now trims the lowest-priority sources (from the
  tail) one at a time, preserving the highest-priority context, instead of dropping
  the whole context block wholesale; only when system + task alone overrun does it
  truncate at the char boundary. Split out `render()` helper.

Tests:

- `memory.rs`: matching memory pushed as a `memory` source; secret-shaped memory
  redacted (`redacted_reason = "secret"`).
- `explain.rs`: injects personal memory when grounding allowed; omits it for a
  cloud provider (via a `CapturingProvider` that records the prompt).
- `prompt.rs`: incremental trim keeps a partial context block instead of dropping
  all sources.
- `allow_memory_grounding: false` added to every existing capability test's deps
  builder + `live_tests.rs`.

Checks: `cargo test --lib ai_capability` 41 passed; `ai_capability::memory` 2
passed; project gate `cargo clippy -- -D warnings` clean. Fixed one
`Range::contains` clippy lint in the new prompt test (only visible under
`--all-targets`).

## COMPLETED: MEM.1.C - memory in search results

- `ResultKind::Memory` (additive, serde `"memory"`) in `models/search_result.rs`.
- `handlers/search/providers.rs::append_memory_results`: reads `scope=personal`
  via `agent_memories_blocking`, ranks with the shared `term_score` (re-exported
  `pub(crate)` from `ai_capability::memory` alongside `PERSONAL_MEMORY_SCOPE`),
  emits up to `MEMORY_LIMIT=6` rows. Called from `append_non_file_results` only
  when `features.ai` is on (memory follows the AI flag).
- `SearchHandler` now holds `config` + `knowledge_store` (`SearchHandlerDeps`
  + `state.rs` registration). The `base_results_to_ui_items` kind→source match
  gained a `Memory => "memory"` arm.
- Frontend: `ResultKind`/`SourceFilter` add `"memory"`; `SearchResultsList`
  `KIND_BADGE` "Mem" (database icon); `useFileActions.launchResult` short-circuits
  memory rows to paste `subtitle` (full one-lined content) into the query.

## COMPLETED: FEAT.GATE - feature-visibility gating (AI + all features)

Developer-directed (overrides the REF.7 freeze). The `features.*` flags already
existed and gated builtins; this wires them through the rest of the stack.

- **Inline AI namespace**: `handlers/ai_capability.rs::call` now refuses
  `capability.call` when `features.ai=false` (mirrors `ai.rs`). Covers
  remember/recall too.
- **Dispatch namespace guard** (`app/dispatch.rs`): `NAMESPACE_FEATURE_GUARDS`
  refuses the whole `note.`/`history.`/`translation.`/`calculator.`/`system.`
  namespace when its flag is off. AI namespaces (`capability`/`ai`/`agent`) are
  intentionally absent — they gate per-route so `ai.check_setup` / model setup
  stay reachable while AI is off (bootstrap-deadlock avoidance). Pure helper
  `route_feature_key` unit-tested.
- **Search providers**: note/history/memory gated by their flags in
  `append_non_file_results`; `model` left ungated (bootstrap).
- **Frontend single source of truth**: new `context/FeatureFlagsContext.tsx`
  (`useFeatureFlags().isEnabled(key)`), seeded `ai/agent=false`, others `true`,
  refreshes on `config-reloaded` (reuses the `useLauncherSettings` pattern).
  Mounted in `AppContainer` inside `IPCProvider`.
- **AI UI gates** (`CommandPalette.tsx`): `aiEnabled` collapses
  `explicitCapabilityMode` / `showSmartNext` / `smartIntentMatch` → `capabilityMode`
  null, hiding cards + keyboard nav + NL fallback; `showCapabilityHintLine`
  gated too.
- **Panel refusal** (`usePalettePanels.ts`): `PANEL_FEATURE` map refuses a
  disabled feature's panel (defense in depth); `model`/`setting`/`nvim_download`
  exempt.
- **Copy**: `features.ai` label `"AI Chat & Assistant"` → `"AI Assistant
  (inline)"` (`settings_schema.rs`); `SettingRow` description updated.

Decision: no new ADR — pure reuse of existing flags + an additive `ResultKind`
value. `features.ai` default stays `false` (opt-in). Calculator has no inline
palette surface (panel-only), so it's covered by the panel + builtin + dispatch
gates.

Checks: `cargo test --lib` 475 passed / 2 ignored; `cargo clippy -- -D warnings`
clean; `npm run lint` clean; `npx vitest run` 170 passed / 1 skipped; `tsc
--noEmit` clean.

## COMPLETED: DECOUP.1 - backend self-registration scaffolding + calculator pilot

ADR-0044 (proposed). New `app/feature_registry.rs`: `AssemblyCtx` (empty,
grows), `FeatureRegistrar::handler()`, `REGISTRARS: &[fn(&mut FeatureRegistrar,
&AssemblyCtx)]`, `register_all(router)`. `handlers/calculator.rs::register`
builds its own leaf `CalculatorManager` + registers the handler. `state.rs`:
removed `calculator_manager` from `ManagerBundle` + `create_managers` + the
explicit `CalculatorHandler` registration + the two imports; `build_command_router`
ends with `feature_registry::register_all(&mut router)`. New + old wiring
coexist (only calculator migrated). Behavior-preserving: `cargo test --lib` 473
passed / 2 ignored; `cargo clippy -- -D warnings` clean.

## COMPLETED: DECOUP.2 - frontend manifest scaffolding + calculator

`features/featureManifest.ts`: `FeatureManifest` type, `FEATURE_MANIFESTS`,
`manifestPanels()` / `manifestPanelGates()`. `features/calculator/manifest.ts`
owns the lazy calculator panel + its `panelGate`. `PanelRegistry.tsx` and
`usePalettePanels.ts` (`PANEL_FEATURE`) dropped the calculator hardcode and
spread the manifest maps. New + old coexist. Behavior-preserving: `npm run lint`
clean, `tsc --noEmit` clean, `npx vitest run` 170 pass / 1 skip.

## COMPLETED: DECOUP.3 - backend feature rollout (7 features)

`AssemblyCtx` grew 5 shared handles (config, event_bus, workspace_manager,
note_manager, history_manager); `register_all(router, &ctx)`. Migrated to
`handlers::X::register`: translation + system_control (leaf — own manager built
in register), note + history + learning_material (handler self-registers, shared
managers from ctx), nvim + system_monitoring (no manager; shared event_bus/
config). `state.rs`: removed 7 `router.register` blocks, dropped `system_manager`
+ `translation_manager` from `ManagerBundle`/`create_managers`, and 9 now-unused
imports. `build_command_router` is now LauncherHandler…WorkflowMemory + the
`register_all` loop. Builtin-command registration + `FeatureSpec` deferred
(builtins shared, registered centrally; spec arrives with DECOUP.4). Behavior-
preserving: `cargo test --lib` 473 passed / 2 ignored; clippy clean. Remaining:
DECOUP.5 (frontend manifests for the other panels), DECOUP.4/.6 (spec-derived
dispatch guard; cross-cutting search/ai_capability/agent) — larger, deferred.

## COMPLETED: DECOUP.5 - frontend panel rollout

`features/<x>/manifest.ts` added for translation / notes / history / system /
system-monitor / nvim (each owns its lazy panel + gate; nvim has no gate).
`featureManifest.ts` `FEATURE_MANIFESTS` lists all 7 (incl calculator).
`PanelRegistry.tsx` now hand-lists only `setting` + `model` and spreads
`manifestPanels()`; `usePalettePanels` `PANEL_FEATURE = manifestPanelGates()`
(all hardcodes gone). `KIND_BADGE` memory row stays central (single AI-owned
kind). Behavior-preserving: lint + tsc clean, 170 vitest pass. Remaining:
DECOUP.4 (dispatch guard / settings derived from FeatureSpec) + DECOUP.6
(cross-cutting search/ai_capability/agent) — larger structural batches.

## COMPLETED: DECOUP.4 - spec-derived dispatch namespace guard

`FeatureSpec { namespace, flag_key }` + `FeatureRegistrar::spec()`; every
`register` fn declares its spec (5 flagged: calculator/translation/system/note/
history; 3 None: nvim/learning_material/system_monitoring). `register_all`
returns the flagged `(namespace, flag)` pairs; `build_command_router` returns
`(CommandRouter, Vec<(&str,&str)>)`; `AppState.feature_namespace_guards` stores
them; `dispatch.rs::route_feature_key(route, guards)` matches the route's
namespace segment (so `system` no longer needs the `.`-suffix trick vs
`system_monitoring`). Retired the hand-kept `NAMESPACE_FEATURE_GUARDS` const;
tests pass a `TEST_GUARDS` slice. Removing a feature now auto-removes its
dispatch guard. Behavior-preserving: `cargo test --lib` 473 passed, clippy clean.

## COMPLETED: DECOUP.6 - declarative search provider chain (cross-cutting scope)

`handlers/search/providers.rs::append_non_file_results` is now a declarative
`(provider, limit, gate)` table driving order + feature gating in one place
(replaces the scattered `if feature_enabled` calls). Per ADR-0044 §2,
`search`/`ai_capability`/`agent` stay centrally assembled (they share managers;
self-registration would violate the shared-infra boundary) and their providers
stay on `SearchHandler`. Behavior-preserving: 473 tests, clippy clean.

DECOUP track complete (.1–.6 at unit level). Removing a feature ≈ delete its
`handlers/X.rs` + `features/X/` + one line in `REGISTRARS` + one in
`FEATURE_MANIFESTS`; minor central touchpoints remain one-line edits
(builtin_cmd registration/`COMMAND_FEATURE_GUARDS`, settings_schema +
default_config `[features]`, frontend `GateKey`/`KIND_BADGE`).

## FIX: settings — stored secret looked unset ("added translate API but says none")

User added `translation.api_key` but it appeared not set. Root cause was
visibility, not the read path: `cfg.get` resolves secrets from the OS keychain
correctly, but the settings panel showed nothing for a stored secret —
`redact_setting_value` masked a set secret to `""`, and `saveValue` reset the
field to `""` after save, with no "is set" indicator. So a saved key looked
blank → "says none". Fix: `redact_setting_value` now returns the `SECRET_MASK`
(`********`) for a SET sensitive key (non-empty but never the real value);
`SettingPanel` keeps the secret input empty (so typing yields a clean key, never
appended onto the mask) and shows a green **Set** badge + "Saved · type to
replace" placeholder when a value is stored; post-save state reflects the mask.
`cargo test` 473 passed, clippy clean; tsc/lint clean, 170 vitest pass. (If the
translate call still fails after showing **Set**, that points to an OS-keychain
read failure — separate follow-up.)

## FIX: FEAT.GATE — disabled feature's command still searchable

User caught a gap: turning a feature off hid its panel/results but its builtin
slash command still appeared in search (the command search provider listed every
builtin; only execution was gated). Fix: lifted the handler's `FEATURE_GUARDS` to
a shared `builtin_cmd::COMMAND_FEATURE_GUARDS` (single source of truth) and
filtered `providers.rs::append_command_results` with it via
`self.feature_enabled(flag)`, so a disabled feature's command is hidden from
search (matching executability). Also extended the guard set to `/system` +
`/system_monitoring` (→ `features.system`), which were never gated — now
consistent with the panel + dispatch gating. Covered commands: `/ai` `/tr`
`/note` `/history` `/cal` `/system` `/system_monitoring`. Behavior-preserving for
enabled features. `cargo test --lib` 473 passed, clippy clean.

## Next

- Manual `npm run tauri dev` smoke (local ollama):
  - MEM.1.B: `explain` / `fix` / `cmd` ground on stored memories; cloud provider
    sends no memory in the prompt.
  - MEM.1.C: stored memories appear as `Mem` rows; activating one pastes content.
  - FEAT.GATE: with `features.ai=false` no capability cards/hint/memory rows, but
    the **model manager still opens** and can download a model; `/setting →
    Features → AI` toggles it back on live. Toggle notes/history/calculator/
    system/translation off → their panel won't open and results disappear.