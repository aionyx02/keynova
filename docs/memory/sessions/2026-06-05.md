---
type: session_log
status: active
priority: p1
context_policy: on_demand
owner: project
updated: 2026-06-05
---

# Session 2026-06-05

## UX.AUDIT - UI/UX consistency pass (developer-directed)

Code-level walkthrough of the whole frontend (no live GUI). Findings, by impact:

1. **Mixed-language UI** — i18n exists (`src/i18n`, zh-TW default) but only 6/56
   components use it; the rest hardcode strings and flip language per panel
   (HistoryPanel zh footer vs ModelPanel en footer).
2. **Stale "AI Chat" copy** — chat removed in REF.8 but ModelPanel + `model.rs`
   `tool_label` still said "AI Chat". → fixed (UX.AUDIT.1 below).
3. **Garbled-text masking** — `SearchResultsList.hasEncodingError('嚙')` swaps
   mojibake titles for "Unavailable text". Investigated every live search-result
   source: Everything (wide `…W` APIs + `\u{FFFD}` skip), `.lnk` app scan
   (`file_stem().to_str()` + FFFD guard), fs walk — all encoding-correct. **No
   current code path decodes search titles as the wrong charset**, so the guard
   is masking a problem the read paths already prevent; likely vestigial or
   stale data in a store. No speculative Rust fix made (cannot reproduce without
   the triggering data). Needs a concrete garbled example to chase further.
4. **No screen-reader live regions** (`aria-live`/`role=status` = 0) on a
   keyboard-first app: search results, AI streaming, Saved/Save-failed, and
   feature-disabled notices are silent to AT.
5. Inconsistent footer-hint format/language across panels; 9 source files carry
   a UTF-8 BOM.

### COMPLETED: UX.AUDIT.1 - drop stale "AI Chat" naming

`model.rs::tool_label` "AI Chat" → "AI Assistant"; ModelPanel.tsx 7 user-facing
strings ("AI Chat is now using …", subtitle, custom-endpoint copy) → inline-AI
wording. No behaviour change. `npm run lint` + `cargo build` clean.

### COMPLETED: UX.AUDIT.2 - screen-reader live regions

Added the first `aria-live` surfaces (was 0). Palette gained one polite
`role=status` sr-only region announcing search state (搜尋中…/N 個結果/找不到
結果/AI 回應產生中…). `role=status` on SettingRow save badge + ModelPanel
footer notice; `role=alert` on NoteEditor save-failed (now via new
`note.saveFailed` i18n key in both locales, replacing a hardcoded English
"Save failed" that sat next to the zh "已儲存"). lint + tsc + vitest
(settings/notes) green.

Remaining UX.AUDIT items (tracked, not yet done): full i18n conversion of the
~44 components that bypass `src/i18n` (UX.AUDIT.3, large); UTF-8 BOM cleanup
(UX.AUDIT.4, cosmetic); garbled-text guard needs a concrete repro before any
root-cause change (see finding 3 above).

### IN PROGRESS: UX.AUDIT.3 - full i18n conversion (developer chose "全面轉換所有元件")

Converting all ~26 components that bypassed `src/i18n` to `useI18n`, batch per
feature area, commit each. Added `src/i18n/format.ts` (`fmt(template, vars)`
with `{token}` placeholders) so interpolated strings stay plain strings and
`I18nKeys` keeps its shape.

- **Batch 1 — ModelPanel** (done): added full `model.*` section to zh-TW +
  en-US (~55 keys incl. title/tabs/notices/footers/bootstrap states); all 3
  views + shell now `useI18n`. Module helpers (`modelSize`, `formatMb`,
  `progressText`, `sourceLabel`, `applyBootstrapState`, `tabLabel`,
  `apiOptionDescription`) take the `model` slice. `tool_label` dropped from the
  subtitle in favour of localized `activeModel`. Browse/Remove footers also
  gained `role=status` live regions. tsc + eslint + lint clean.

- **Batch 2 — SettingPanel + SettingRow** (done): added `settings.*` section
  to both locales (section labels + effect hints + feature descriptions as
  `Record<string,string>` maps, reload/applied notices, secret placeholders,
  Set/Saving/Saved badges). `sectionDisplayLabel` takes the labels map;
  `FEATURE_DESCRIPTIONS` const removed in favour of `s.featureDescriptions`.
  tsc + eslint + vitest (settings) clean.

- **Batch 3 — AI capability cards** (done): shared `capability.*` section in
  both locales (~75 keys: close/cancelled/suffixes + per-card footers, bodies,
  buttons). Command/Recall/Memory/List/Answer cards all use `useI18n`;
  AnswerCard's module `LABEL_TITLE` map became a locale-derived local. 44
  capability vitest cases still green (jsdom → en-US, strings unchanged).

- **Batch 4 — palette secondary actions + suggestion footers** (done): `palette.*`
  i18n section (footer-hint verbs, secondary-action labels/hints map, inline
  rename/move placeholders, filter-empty copy). `buildSecondaryActions(result,
  labels)` now takes the localized action map; both call sites (`useDerivedView`,
  `useKeyboardNav`) feed it via `useI18n`. SecondaryActionMenu / CommandSuggestions
  / ArgsSuggestionsList / EmptyFilterState footers localized — this is the
  "footer hint flips language per panel" symptom the developer flagged. 114
  palette/utils vitest cases pass.

- **Batch 5 — SearchResultsList** (done): kind badges (App/File/Dir/Cmd/Note/
  Hist/AI/Mem → `search.kinds`), the `嚙` encoding fallbacks, and the open/
  preview/actions footer verbs now localized via the extended `search.*` section.

### COMPLETED: UX.AUDIT.3 final i18n sweep + UX.AUDIT.4 BOM cleanup

Finished the remaining i18n conversion from `docs/tasks/ux-audit.md`:

- Overlays: `CheatsheetOverlay`, `OnboardingTour`.
- Panels/surfaces: `TerminalPanel`, `LearningMaterialPanel`,
  `SystemMonitoringPanel`, `NvimDownloadPanel`, `PreviewPane`,
  `FloatingWindow`, `ErrorBoundary`, `CommandResultArea`, `RankTooltip`.
- Stragglers found by visible-text scan: `CapabilityHintLine`,
  `MouseControlOverlay`, `CalculatorPanel`, `HistoryPanel`, `SystemPanel`,
  `TranslationPanel`, search footer diagnostics, `PaletteInputBar`,
  `FilterChips`, `EmptyStateCTA`, `CommandPalette` live-region text and
  generated-command safety copy.

Locale additions landed in `src/i18n/zh-TW.ts` + `src/i18n/en-US.ts`; class-based
`ErrorBoundary` now uses `resolveI18n()` while hook components use `useI18n()`.
The final JSX visible-text scan only reports key glyphs (`Enter`, `Esc`,
`Up/Down`), acronyms/brands (`AI`, `CPU`), route/type declarations, and the
settings config path.

Also stripped UTF-8 BOM from the 9-file UX.AUDIT.4 list and verified no listed
file still starts with BOM.

Validation:

- `npm run build` ✅
- `npm run lint` ✅
- `npm run test` ✅
- `docs:refresh` initially failed only because code changed without project-state
  docs; this section plus `docs/tasks/ux-audit.md`/`active.md` updates close that
  guard requirement.

## COMPLETED: BUGFIX.SECRET - keyring credential-store backend (secrets now persist)

Translation (and every other sensitive) API key read back empty even though the
settings UI showed it as `Set`, so `translate` reported "Translation API key not
configured". Root cause was a misconfigured dependency, not app logic.

### Root cause

`src-tauri/Cargo.toml` declared `keyring = "3"` with no credential-store feature.
keyring 3.x compiles fine without a backend but silently falls back to an
in-memory **mock** keystore. `secret_store::store_secret` returned `Ok` and the
config map kept the `keyring:keynova:<key>` reference (so the UI `Set` badge and
the redaction mask both showed "configured"), but the value never reached the OS
keychain. On the next read — or the next process — `keyring::Entry::get_password`
hit a fresh/empty mock store, so `resolve_secret_value` returned `""` and
`runtime_config().api_key` was empty.

### Fix

Wire each platform to its native backend via target-specific dependency tables so
non-target deps (Linux dbus/secret-service) aren't built elsewhere:

- base: `keyring = { version = "3", default-features = false }`
- `cfg(windows)`: `features = ["windows-native"]` (pulls `windows-sys` + `byteorder`)
- `cfg(macos)`: `features = ["apple-native"]`
- `cfg(linux)`: `features = ["sync-secret-service", "crypto-rust"]`

Verified `cargo tree -p keyring` now shows `byteorder` + `windows-sys` (the
windows-native backend) instead of the mock store. `cargo build`, `cargo clippy
-- -D warnings`, and the `secret` test suite all pass.

### Follow-up for the user (one-time)

Any secret stored before this fix only ever lived in the now-discarded mock store,
yet `config.toml` still holds its `keyring:keynova:<key>` reference. The UI will
keep showing `Set` while the real keychain is empty, so the key must be
re-entered once via `/setting → translation.api_key`; after that it persists to
Windows Credential Manager and round-trips correctly.

### Relationship to 9529ac8

Commit `9529ac8` (show stored secrets as `Set` instead of blank) was the display
half. This is the functional half — the value now actually persists. Governed by
ADR-0041 (secret-at-rest); no new ADR (keyring was already a dependency, only its
backend feature was enabled).

## COMPLETED: PRODUCT.0 - positioning lock (README non-goals + core/parked + project.md align)

First batch off the new `docs/tasks/product-roadmap.md`. Developer selected the
PRODUCT.0 direction explicitly, which lifts the roadmap freeze for the
positioning-docs layer only (PRODUCT.1+ stay frozen). Docs-only, **no runtime
change, no ADR** (pure consolidation of existing principles + additive copy).

### Changes

- `README.md`: added **「定位與非目標」** (canonical EN/zh statement + four
  explicit non-goals: general-launcher parity, AI-chat center, autonomous-agent
  mainline, catch-all suite) and **「核心 vs 選用」** (core daily path vs parked
  feature-gated surfaces) with a pointer to `docs/tasks/product-roadmap.md`. The
  existing 「計畫中」 signing/updater/keychain table is unchanged.
- `docs/project.md`: Product line re-aligned from generic "productivity launcher"
  to "keyboard-first local workflow entry for technical workers" (search-first
  dispatcher, AI as inline capability) + explicit non-goals line; Current Strategy
  swapped "Feature-first, refactor-second" for search-first / approval-aware to
  match `memory/current.md`.
- `docs/tasks/backlog.md`: ticked `PRODUCT.0`.

### Validation

- `npm run docs:refresh` (workbench-sync + guard-size/schema + audit-frontmatter
  + narrative-check + guard).
- No `src/` or `src-tauri/` changes; `git status` is docs/README only.

### Done criteria met

- README describes the product in one screen without promising broad launcher or
  AI-agent behavior.
- Docs distinguish core/MVP workflow from optional/parked features.
- Zero runtime behavior change.

## COMPLETED: UX.AUDIT.5 - garbled-text fallback (raw path instead of "Path unavailable")

Took the documented fallback for the `嚙` mojibake guard since the root cause is
still unrepro'd. The detail line in `SearchResultsList` previously masked any
encoding-flagged subtitle/path to `search.pathUnavailable`, which hid the path and
left the row unusable. Now it falls back to the raw `result.path`, so an affected
row stays actionable (open / reveal still resolve off `result.path`).

### Changes

- `src/features/command-palette/SearchResultsList.tsx`: `detail` fallback is
  `result.path || detailSource` instead of `t.search.pathUnavailable`. Title still
  masks to `t.search.unavailableText` (the path beneath identifies the row).
- `src/i18n/{zh-TW,en-US}.ts`: removed the now-dead `search.pathUnavailable` key
  (`I18nKeys = typeof zhTW`, so dropped from both locales together).
- `src/features/command-palette/SearchResultsList.test.tsx`: replaced the REF.6
  placeholder with 3 row-level cases (garbled detail → raw path; garbled title →
  masked title + path beneath; clean → untouched). The `scrollIntoView` polyfill
  in `vitest.setup.ts` was already in place for exactly this.

### Validation

- `npx vitest run` → 173 passed (incl. the 3 new cases).
- `npm run lint` clean; `npm run build` (tsc + vite) green.

### Still open (non-urgent)

Root cause unresolved — no live read path produces `嚙`. `hasEncodingError` may be
vestigial; deleting it outright needs a concrete garbled sample from the user.

## COMPLETED: UX.AUDIT.2b - a11y live-region deepening for AI capabilities

Continuation of UX.AUDIT.2 (developer-directed, not a new feature). The palette's
single polite live region announced `search.aiGenerating` when a capability
started but returned `""` on completion (`showCapabilityResult` swallowed it), so
screen-reader users heard the start and then silence; failures were entirely
silent.

### Changes (`src/components/CommandPalette.tsx` + i18n)

- New `activeCapabilityOutcome` derived centrally from whichever capability is
  active — `capabilityStream` (explain/summarize/fix), `genCommandState` (cmd),
  `suggestNextState` (next), `rememberState` (remember), `recallState` (recall).
  Both status enums (`CapabilityStreamStatus`, `CapabilityRunStatus`) converge on
  `complete` / `error`, so a single `.find(active)` covers all 5.
- `liveRegionText` now returns `search.aiReady` on completion (polite region).
- New assertive `role=alert` / `aria-live=assertive` sr-only region announces
  `search.aiError` on failure (mirrors the NoteEditor save-failed precedent). The
  message is generic — the visible card body still carries the detailed error, so
  AT does not read long raw error strings assertively.
- Added `search.aiReady` / `search.aiError` to both locales.

Centralized rather than adding a live region to each of the 5 capability cards
(DRY, matches the existing "single polite live region" design comment).

### Validation

- `npm run lint` clean; `npm run build` (tsc + vite) green; `npx vitest run` → 173
  passed (no regressions).
- Announcement is event-driven (capability streaming over Tauri events), so per
  the project norm it is **not** unit-tested; verify under `tauri dev` with a
  screen reader: run `explain <code>` and confirm "AI response ready" on finish,
  and an error case announces "AI request failed".

## DECISION: conditional partial unfreeze of the REF.7 freeze

Developer chose option A after I laid out the freeze gate. The REF.7 freeze could
not be closed by code alone: its remaining gates are a user-action bench
(`REF.7.D`, qwen2.5:7b 4.7 GB pull) plus inherently time/usage-based observation
items (one release cycle with no P0 regression = 1 minor tag + 14 days per
ADR-0029 §2.5; idle RSS < 150 MB@10min / < 200 MB@1h). So the freeze was
self-perpetuating without an explicit developer call.

Decision: **conditional partial unfreeze.**

- **Unfrozen:** `PRODUCT.1` search-core (ranking, unified-result audit, workspace
  search, command discovery, terminal workflow, file actions/preview, dev
  utilities, keyboard/perf gate). Rationale: the freeze exists so new product work
  does not mask refactor regressions; the REF.7 gates measure the **AI hot path**,
  and PRODUCT.1 does not touch the AI capability layer — they are orthogonal.
- **Still frozen:** `PRODUCT.2` (AI capabilities) and any AI-capability/agent-
  touching feature, until `REF.7.D` lands and the observation window closes.
- **Demoted to non-blocking tracking items:** `REF.7.D` bench + the
  observation-window items (no longer a queue gate).
- Individual risky `PRODUCT.1` sub-items (destructive file ops, kill-port, shell
  handoff) still pass their own approval/ADR gates.

No new ADR: this relaxes a process work-freeze recorded in the state docs, not
ADR-0029 scope (ADR-0029 stays `accepted`, unchanged). Codified in `active.md`,
`current.md`, `blocked.md`, `backlog.md`, and `product-roadmap.md`. Next:
scope `PRODUCT.1.A` (ranking baseline) into `product-roadmap.md` before coding.

## COMPLETED: PRODUCT.1.A - ranking baseline (workspace_context + config terms)

First implemented batch off the unfreeze. Audit found the live ranking model
(`base + recency + frequency`, deterministic + unit-tested, shown in RankTooltip)
plus a workspace hard-filter; the roadmap formula was missing workspace_context
as a *score*. Added it as a bounded additive term rather than reworking scoring.

### Changes

- `ScoreBreakdown` (Rust `models/action.rs` + TS `types/search.ts`) gains
  `workspace_boost` and `config_boost`.
- `handlers/search/ranking.rs`: pure `workspace_boost(source, path, project_root)`
  (+20 for file/folder/app under root; case/separator-insensitive; 0 for non-file
  sources or unset root) and `config_boost(source, path)` (+6 for README /
  manifests / `.toml|.yml|.yaml|.ini|.cfg`). 6 new unit tests.
- `handlers/search.rs::apply_rank_boost`: resolves `project_root` once from the
  workspace manager and adds both terms; breakdown carries all five components.
- `RankTooltip.tsx`: workspace/config rows (rendered when non-zero) + folded into
  total; `rank.workspace` / `rank.config` in both locales. New 2-case test.
- Fixed the `unified_result.rs` ScoreBreakdown test literal for the new fields.

### Design notes

- workspace_boost = +20 (between recency max 25 and frequency max 40),
  config_boost = +6 — both developer-approved tunables.
- Safe default: unset `project_root` → workspace_boost 0 for all rows, zero
  behavior change. The boost mainly matters in `:global` mode, for app results,
  and when no hard filter is active.
- No ADR (additive term on an already-additive model). A future weighted/
  normalized rewrite would need one.

### Validation

- `cargo test` 482 passed + `cargo clippy -- -D warnings` clean.
- `npm run build` + `npm run lint` green; `npx vitest run` 180 passed.

Deferred (per scope, not regressions): action_success (frequency proxy),
risk_penalty (→ 1.D/E), noise_penalty (→ 1.C).

## COMPLETED: PRODUCT.1.B - unified-result contract audit

Audit confirmed the contract is already uniform: every source crosses IPC as
`UnifiedResult` via the `From<UiSearchItem>` shim with stable id, preserved
kind/path/score+breakdown, one primary `ActionChip`, lazy preview, and
`secondary_action_count`. Row-level risk is `ConfirmRequirement::none()` by design
(risk lives on actions/capabilities). Frontend per-kind branches
(`secondaryActions.ts`, memory paste) are legitimate distinct actions, not
duplicated behavior — nothing to remove.

Done this batch: added `ui_search_item_shim_covers_every_source_kind` in
`models/unified_result.rs` asserting all 8 `ResultKind`s map with stable
id/kind/path/score, one primary action, no row-level confirm — satisfies the
"tests cover at least one result from each source kind" criterion.

Deferred finding (documented, not a regression): the shim wraps every source as
`ResultSource::File`, leaving `ResultSource::BuiltinCommand`/`Other` unused by the
search path. A proper per-source `ResultSource` rework is a data-contract change
(governance §7, ADR-gated) that ripples through `unifiedToLegacy`/`unifiedKindOf`
— out of audit scope. Revisit only if a source needs semantics the File wrapper
cannot express.

Validation: `cargo test` (the 3 shim tests + suite) green; no behavior change.

## COMPLETED: PRODUCT.1.C - workspace-aware search + noise suppression

Audit: workspace coverage already works (1.A `workspace_boost` + the
`resolve_workspace_filter` hard filter; workspace switch reads root per search =
no restart). Real gap was noise — the IgnoreWalk fallback respects
`.gitignore`/hidden, but the primary providers (Everything/Tantivy) do not, so
generated dirs dominate. Implemented the deferred 1.A `noise_penalty`.

Developer chose **demote -30** (vs hide): generated/dependency dirs are demoted,
not hidden, so they stay reachable and the tooltip explains why (`noise -30`).

### Changes

- `handlers/search/ranking.rs`: pure `noise_penalty(source, path)` (-30 for a
  file/folder whose path has a denylisted segment; segment-exact; file source
  only) + `NOISE_SEGMENTS` const. 3 new tests incl. the "clean src/index.js
  outranks node_modules same-name even with higher base" regression fixture.
- `ScoreBreakdown.noise_penalty` (Rust + TS); `apply_rank_boost` subtracts it.
- `RankTooltip` rose noise row (shown when non-zero) + `rank.noise` both locales.

### Deferred (per scope)

Query-aware exemption (don't penalize when the query names a noisy segment;
needs query threaded into scoring), indexer-level exclusion (Everything is a
system index we don't own), workspace-identity badge (UI polish).

No ADR (bounded additive penalty, same class as 1.A). Validation: cargo test 486
+ clippy clean; build/lint + 181 vitest green.

## COMPLETED: PRODUCT.1.D - project command discovery (copy-only MVP)

Developer chose copy-only + 4 manifests. New feature — nothing discovered project
scripts before (builtin registry = app commands only).

### Changes

- `core/project_commands.rs`: `discover(root)` reads `package.json` scripts
  (`npm run <name>`), `Cargo.toml` (presence → standard cargo intents), `Makefile`
  targets, `justfile` recipes; `normalize_intent` + `is_risky` + `ProjectCommand`
  (carries `risky` for 1.E). Best-effort, fs-read only, non-recursive. 4 tests.
- `handlers/search/providers.rs`: new ungated `append_project_command_results`
  (chain entry #2) — reads the workspace `project_root`, filters by query against
  command/intent, emits `command` rows with the `projectcmd://` path scheme,
  subtitle `source · cwd`, label "Copy". No root ⇒ no rows.
- `useFileActions.launchResult`: intercepts `projectcmd://` rows → clipboard copy
  (mirrors the memory paste short-circuit). No execution path.

### Deferred (per scope) → 1.E / follow-ups

Run / send-to-terminal + high-risk confirmation gate (1.E owns execution),
`docker-compose.yml`, yarn/pnpm/bun detection, a dedicated `features.project_commands`
flag (MVP is self-gating: only fires when a project_root + manifests exist).

No ADR (copy-only reuses the declarative provider + UnifiedResult/ActionChip; reads
are within search scope). Validation: 490 cargo + clippy clean; build/lint + 181 vitest.

## COMPLETED: PROJECT_ROOT.wire - activate dormant workspace features

Manual verification of 1.A–1.D surfaced that nothing populated
`WorkspaceManager.project_root` (no frontend setter; `withGlobalTauri` off ⇒ no
console workaround), so 1.A `workspace_boost`, the pre-existing workspace search
filter, and 1.D project commands never fired end-to-end. Tasks 1–4 verified OK by
the developer first.

### Changes

- `core/project_commands.rs::detect_project_root(start)`: walks cwd ancestors for
  a project marker (`.git`/`Cargo.toml`/`package.json`/`go.mod`/`pyproject.toml`/
  `Makefile`/`justfile`/`pom.xml`/`build.gradle`/`.hg`/`.svn`), **stops at `$HOME`
  and drive root** so a stray home marker can't make the whole home tree the
  project. `home_dir()` via `USERPROFILE`/`HOME` (no new dep). 2 tests (nearest
  marker; none without markers — the bounded walk is what makes the negative test
  deterministic on a real FS).
- `WorkspaceManager::set_project_root_if_unset(root)`: in-memory, preserves any
  existing/persisted value, ignores blank. 1 test.
- `app/state.rs`: at startup, detect from `current_dir()` and set-if-unset.

### Behavior change

Launching inside a project scopes file search to it (`:global` escapes); 1.D
command rows appear; `workspace_boost` is observable in `:global`. No project at
cwd ⇒ no-op (global search as before). Known limit: detects launch cwd once; does
not follow the foreground window's project (deeper follow-up).

No ADR (local read + existing workspace field). Validation: 493 cargo + clippy
clean. Backend-only; no frontend change.

### Re-verify (task 5, now unblocked)

Run `npm run tauri dev` from the keynova repo, then: (a) type `test`/`build` →
`cargo test` / `npm run build` rows appear, Enter copies; (b) `:global <name>`
where the name exists in and outside the repo → in-repo file ranks higher, tooltip
`workspace`; (c) plain search is now repo-scoped.

## COMPLETED: RANK.tune - lower boost magnitudes (dogfood)

Developer dogfood: searching `keynova` for the folder, it did not surface — the
boosts (esp. recency/frequency) buried strong exact-name matches under
frequently-used files. Lowered the weights so `base` leads and boosts are
tie-breakers:

- `recency` 25/15/8 → **12/8/4**; `frequency` `count.min(10)*4` (max 40) →
  `*2` (max 20) — `search_manager.rs::rank_boost_breakdown` + its 3 tests.
- `workspace_boost` 20 → **12**; `config_boost` 6 → **4** — `ranking.rs` + tests.
- `noise_penalty` -30 unchanged (a penalty helps surface real targets).

Positive boost ceiling ~48 vs typical file base ~95, so relevance dominates.
Tunable — if the folder still ranks low after re-test, drop further or special-case
exact-name matches in `base`. 493 cargo + clippy clean; backend-only.

## COMPLETED: UX.POLISH.1 - launcher first-screen visual polish

Continuation of the UX pass on `feature/product-0-positioning`, aligned with the
PRODUCT.0 positioning: Keynova should feel like a keyboard-first workflow tool,
not a chat surface or a decorative product page. Scoped to the palette first
screen and shared visual tokens only.

### Changes

- `src/index.css`: shifted the UI tokens from blue-heavy glass toward neutral
  graphite surfaces with teal/warm accents; added shared launcher/input/filter/
  result/empty-state classes.
- `PaletteInputBar`: collapsed the prior subtitle/footer helper copy into a
  single focused command surface, with the input as the dominant element and a
  compact mode pill.
- `FilterChips`: stabilized filters into a single horizontal strip so chips no
  longer wrap and push results downward.
- `SearchResultsList` / `CommandSuggestions`: stabilized palette row height,
  strengthened selected-row affordance, and moved kind/action badges out of the
  title line for faster scanning.
- `EmptyStateCTA`: reworked the no-results panel into a compact action surface
  with fixed icon treatment and truncation-safe buttons.

### Validation

- `npm run lint` clean.
- `npm run build` clean (tsc + Vite production build).
- `npm run test -- SearchResultsList` clean (3 tests).
- Browser preview at `http://127.0.0.1:5173`: initial palette and no-results
  state visually checked; visible-element overflow scan reported only the
  expected `sr-only` live region.

## COMPLETED: UX.POLISH.2 - sticky empty-palette starts + action trust

Second UI polish pass on `feature/product-0-positioning`, focused on making the
launcher feel worth reopening: empty state should offer useful return paths, and
secondary actions should feel clear enough to trust repeatedly.

### Changes

- Added `StarterActionsLine` on the empty palette with core return paths:
  `/help`, `/setting`, `/note`, `/model`. These are independent of the AI feature
  gate, so users always see a small set of useful next actions.
- Upgraded `CapabilityHintLine` from a static prefix list to clickable quick
  starts (`next`, `cmd`, `remember`, `recall`, `fix`, `explain`, `summarize`).
  Picking one fills the query and returns focus to the input.
- `CommandPalette` now uses one shared suggested-query handler for starter and
  capability entry points, resetting smart-dismiss state and preserving keyboard
  focus.
- `SecondaryActionMenu` now shows a clearer target header (title + detail) and
  medium/high risk pills, making rename/move/delete confirmation state easier to
  read before repeated use.
- Added i18n labels for starter actions, capability quick-start labels, and
  secondary action risk levels.

### Validation

- `npm run test -- CapabilityHintLine StarterActionsLine` clean (7 tests).
- `npm run lint` clean.
- `npm run build` clean (tsc + Vite production build).
- Browser DOM verification at `http://127.0.0.1:5173`: empty palette exposes
  Help/Settings/Notes/Model buttons; clicking Settings fills `/setting` and
  switches the input to command mode. Screenshot capture timed out in the browser
  plugin during this pass, but DOM interaction checks succeeded.

## COMPLETED: DOCX.PREVIEW - file.preview extracts docx body text

Fixed `.docx` files showing as binary/no-preview in the launcher preview pane.
The preview backend previously classified docx by sniffing the zip container,
which correctly looked binary but was not useful for users.

### Changes

- Added the `zip` crate with deflate support for Office Open XML containers.
- `core::preview` now detects `.docx`, opens `word/document.xml`, extracts
  bounded text from `<w:t>` runs, preserves paragraph breaks/tabs, decodes common
  XML entities, and reuses the existing redaction/truncation policy.
- `file.preview` now special-cases `.docx` before generic classification and
  returns `kind: "text"` plus the docx MIME type, so the existing `PreviewPane`
  renders it without a frontend branch.
- Added unit coverage at both extraction and `file.preview` JSON layers.

### Validation

- `cargo test --manifest-path src-tauri/Cargo.toml preview -- --nocapture` clean.
- `cargo test --manifest-path src-tauri/Cargo.toml` clean: 476 passed, 2 ignored.
- `cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings` clean.

## COMPLETED: PREVIEW.FAILURE - stop infinite preview skeletons

Small follow-up found while fixing docx preview. `useFilePreview` swallowed
`file.preview` errors, but `previewLoading` was derived only from the absence of
`previewByPath[path]`; a failed preview therefore showed the skeleton forever.

### Changes

- `useFilePreview` now caches failed preview paths alongside successful previews.
- `useDerivedView` treats a failed path as not loading, so `PreviewPane` falls
  through to the existing "No preview available" state.
- Added `useFilePreview.test.tsx` coverage for rejected preview requests.

### Validation

- `npm run test -- useFilePreview` clean.
