---
type: session_log
status: active
priority: p2
context_policy: on_demand
owner: project
---

# Session 2026-06-10

## COMPLETED: CI.HARDEN + REF.8 merge to main

Developer-directed close-out: land the bundled `feature/ci-supply-chain-hardening`
branch (REF.8 A/B/C + post-REF.8 cleanup + CI.HARDEN, 5 commits, forked from
`main`) onto `main`. Merge target confirmed as `main` (not `dev`): the branch
forked from `main`, recent close-out work targets `main` directly, and `dev` is
153 behind / 2 ahead (abandoned; policy forbids main→dev). Local `--no-ff`
merge; push + branch protection left to the developer (`gh` not authenticated).

### Pre-merge validation (all green)

`cargo test` 460 pass / 2 ignored, `cargo clippy -- -D warnings` clean,
`npm run lint` clean, `npm run test` 218 pass (34 files), `npm run build` green,
`npm run docs:refresh` guards green. Working tree was clean at branch tip.

### Merge

`--no-ff` merge commit `8e49dd1` over `f3a9e50`, bringing in `6f871cf` (legacy
agent removal), `2d35f53` (nvim + mouse_control cut), `2d65803` (REF.6/7/8
converge), `7c73eea` (dead `useAgent` + clippy `--all-targets`), `deb147e`
(Dependabot + CodeQL + runbook). `main` is now 6 ahead of `origin/main`,
**not yet pushed**. Topology check: `main...feature/ci-supply-chain-hardening`
= `1  0`.

### Governance / ADR note

The merge carries REF.8's ADR status flips (0016/0023/0026 superseded, 0022
partial), scheduled by the already-accepted ADR-0029 §14. Developer plan
approval is the governance §3 merge-time confirmation for those flips.

### Handoff to developer (not run here — `gh` unauthenticated)

```bash
git push origin main
gh auth login   # admin token
gh api -X PUT repos/aionyx/keynova/branches/main/protection \
  -H "Accept: application/vnd.github+json" \
  -f "required_status_checks[strict]=true" \
  -f "required_status_checks[contexts][]=Analyze (javascript-typescript)" \
  -f "required_status_checks[contexts][]=Analyze (rust)" \
  -f "required_status_checks[contexts][]=docs" \
  -F "enforce_admins=false" \
  -F "required_pull_request_reviews[required_approving_review_count]=1" \
  -F "restrictions=null"
```

Status-check context names appear in GitHub's selectable list only after the
CodeQL + docs-guard jobs have reported once on a `main` push/PR.

### Out of scope (flagged, not actioned)

- `dev` 153-behind rehabilitation (policy forbids main→dev; needs separate call).
- Deferred refactors: `agent`-named survivors rename, `ai_manager.rs` per-provider
  split, DECOUP/ADR-0044 self-registration finish.
- `origin/dependabot/cargo/...` PR (GitHub security update, independent of this branch).

## IN PROGRESS: SEC-PERF - security + system-perf hardening pass

Developer-directed: "open a branch, handle security + system perf optimization."
Branch `hardening/security-perf` (from `main`@`ac3986c`). Scope pinned after a
survey via [[feedback_minimal_scope]] / [[feedback_task_persistence]] (task-doc
`docs/tasks/security-perf-hardening.md` written before coding). Survey's headline:
**most of the requested "security work" is already implemented** — the real gap
was `security.md` drift. Not committed yet; validation pending.

### Key finding — keychain migration already shipped

ADR-0041 (secret-at-rest → OS keychain) is **fully implemented** but its doc
status is still `proposed`:
- `core/secret_store.rs` uses `keyring` 3.x with per-OS native backends
  (`windows-native` / `apple-native` / `sync-secret-service`), Cargo.toml + lock.
- `config_manager.set()/get()` route `is_sensitive_key` values through the
  keychain; `config.toml` keeps only a `keyring:keynova:<key>` reference.
- `migrate_plaintext_secrets_to_keychain()` runs on init (one-time plaintext→store
  move). So the user-selected "API key → keychain" item was already done.
- **Flag for developer (governance §3):** code is live but ADR-0041 is `proposed`
  — same pattern as ADR-0039. AI did **not** flip the status; developer should
  confirm → accepted (or revert if it shipped ahead of intent).

### SEC.1 — dependency vuln baseline (2026-06-10)

- `npm audit`: **0 vulnerabilities**.
- `cargo audit` (installed `cargo-audit` 0.x): **0 vulnerabilities** across 716
  deps. 18 warnings, all **transitive + not directly fixable**: Tauri's Linux
  GTK3 stack (`atk`/`atk-sys`/`gdk`/`gdk-sys`/`gtk`/`glib` — `unmaintained`, plus
  `glib` `VariantStrIter` `unsound` RUSTSEC-2024-0429) and `unic-*` (unmaintained).
  Fix path is upstream (Tauri off GTK3); Dependabot/CodeQL track ongoing. No
  action this batch.

### SEC.2 — security.md reconciliation to post-REF.8 + keychain reality

Docs-only, no runtime change. Dropped nvim everywhere (cut in REF.8): §3.2 path
table row, §5.1 network table row, §9 checksum row, and the PREFLIGHT
allowed-writes "Nvim" mention. Rewrote §9 known-limits: keychain DONE (→ §4.5,
ADR-0041 proposed), CSP DONE with residual `style-src 'unsafe-inline'`, API-key
row folded into keychain. Added new §4.5 (secret at-rest via OS keychain).
`connect-src` `api.anthropic.com`/`api.openai.com` confirmed legitimate
(`ai_manager` + `core/network_policy.rs`, ADR-0011) — kept.

### PERF.1 — measurement baseline (measure-first; NO optimization)

Release binary (`cargo build --release`, strip+LTO profile): GUI `tauri-app.exe`
**≈ 22.5 MB**, CLI `keynova.exe` **≈ 815 KB**. Frontend `dist/` (gzip): eager
`index` 160 KB (49 KB), `react-vendor` 192 KB (60 KB), `index.css` 46 KB (9 KB)
≈ **~118 KB gzip initial**; largest chunks are **lazy** — `markdown-vendor`
335 KB (101 KB), `terminal-vendor` 331 KB (83 KB) load only with their panels.
Memory (PERF.1.FU) ~80 MB Private-WS stands. **Conclusion: no obvious low-hanging
perf debt; biggest levers if pursued = markdown/terminal vendor chunks (already
lazy) and the 22.5 MB GUI binary.** Handed back to developer to pick a target.

### Nice-to-have / deferred (not done)

- CSP `style-src 'unsafe-inline'` removal (needs React inline-style nonce/hash;
  ADR). Concrete perf optimization (gated on baseline choice above).
