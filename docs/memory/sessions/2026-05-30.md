---
type: session_log
status: active
priority: p2
updated: 2026-05-30
context_policy: on_demand
owner: project
---

# Session 2026-05-30

## Highlights

- Read the local Claude project memory/session files for this repo so the work could continue from the persisted branch context instead of restarting from scratch.
- Continued the user-directed renderer-memory pass on `feature/ref-7-quantitative-gates`, focusing on the controllable WebView2 renderer bundle/DOM cost rather than the browser-process baseline or the intentional PTY/prewarm overhead.
- Finished the lazy-loading follow-through by removing the last eager panel import in `PanelRegistry`: `SettingPanel` now resolves only when the settings panel is opened.
- Fixed the single frontend regression introduced by lazy markdown loading: `CapabilityAnswerCard.test.tsx` now waits for the async markdown chunk instead of assuming eager render timing.
- Extended the pass into native Windows memory controls once bundle wins flattened out: terminal prewarm now respects `performance.low_memory_mode`, default Ollama keep-alive shrinks in that mode, and the launcher now demotes WebView2 to low memory usage while hidden/inactive.
- Re-ran the launcher memory path against a live repo-local debug `tauri-app.exe`: a fresh cold hidden launch measured `63.7 MB WS / 124.7 MB PM`, `keynova start` handoff returned in about `10-13 ms`, and warm toggles stayed around `90-106 MB WS`.
- Ran a direct `cargo build --release` binary pass as a candidate closure baseline. PM stayed near the debug reading (`124-129 MB`), but WS remained much higher (`273.6 MB` hidden after warm hide / `334.7 MB` active), so the release path stays open.
- Refreshed `README.md` so the public-facing summary matches the latest memory re-check, `low_memory_mode` behavior, and the current `REF.7` / legacy-agent status.
- Added a dedicated Windows memory-usage table to `README.md` so the debug vs direct-release measurements are visible without having to read the session log.

## Detailed Notes

- `src/components/panel/PanelRegistry.tsx`
  - Replaced the eager `SettingPanel` import with `React.lazy(() => import("../../features/settings/SettingPanel"))`.
  - `CommandResultArea.tsx` already had a `Suspense` boundary around `PanelComponent`, so no additional runtime wiring was required.
- `src/features/ai-capability/CapabilityAnswerCard.test.tsx`
  - Changed the markdown assertion from synchronous `getByText("bold")` to async `findByText("bold")`.
  - This preserves the lazy markdown split while keeping the test aligned with the real browser behavior.
- `src-tauri/src/handlers/feature.rs` and `src-tauri/src/handlers/terminal.rs`
  - Wired `ConfigManager` into both handlers so `performance.low_memory_mode` can skip terminal prewarm both on `feature.activate("terminal")` and after `terminal.open`.
- `src-tauri/src/managers/ai_manager.rs`
  - `resolve_ai_runtime_config` now shortens the default Ollama keep-alive from `5m` to `0s` when `performance.low_memory_mode = true`, while preserving any explicit user override.
- `src-tauri/src/app/window.rs`
  - Centralized launcher show/hide behavior.
  - On Windows, the launcher now calls WebView2 `SetMemoryUsageTargetLevel(Normal)` when shown/focused and `SetMemoryUsageTargetLevel(Low)` when hidden or allowed to blur-dismiss.
- `src-tauri/tauri.conf.json`
  - Added launcher-specific `additionalBrowserArgs` to disable non-essential WebView2 features and GPU acceleration for the transparent always-on-top launcher window.
- Repo-local runtime validation
  - Recycled the running debug `tauri-app.exe` through the local `keynova.exe` control CLI so the measurements came from a fresh cold hidden launch instead of the older long-lived instance.
  - Used the registered global `Ctrl+K` toggle to force a real hide transition after wake-up; the earlier "focus Notepad" blur probe proved too noisy to trust as the final number.
  - Rebuilt `src-tauri\target\release\tauri-app.exe` with `cargo build --release --manifest-path src-tauri/Cargo.toml`, swapped over to the release binary long enough to measure it, then restored the repo-local debug app so the workspace returned to its prior state.

## Verification

- `npm run lint`
- `npm run test`
- `npm run build`
- `npm run guard:size`
- `cargo test --manifest-path src-tauri/Cargo.toml --lib`
- `cargo clippy --manifest-path src-tauri/Cargo.toml --lib -- -D warnings`
- `cargo build --manifest-path src-tauri/Cargo.toml`
- `cargo build --release --manifest-path src-tauri/Cargo.toml`

All passed on this branch after the follow-up changes.

## Measurement Notes

- Production build after the follow-up emitted `assets/index-*.js` at `104.28 kB` gzip `30.00 kB`.
- This improves on the prior lazy-loading pass baseline (`~112.22 kB` main chunk) by moving `SettingPanel` into its own `8.16 kB` lazy chunk.
- `react-vendor` (`192.50 kB`), `terminal-vendor` (`330.51 kB`), and `markdown-vendor` (`334.65 kB`) remain intentionally split from the hot path and only load on demand.
- Before the native WebView2 pass, the isolated Keynova app tree measured about `464.4 MB WS / 276.7 MB PM`.
- After `additionalBrowserArgs` GPU disable, the same app tree dropped to about `343.8 MB WS / 125.9 MB PM`.
- After the WebView2 memory target level hook:
  - hidden steady state (30s after cold hidden launch): `77.7 MB WS / 126.7 MB PM`
  - after re-activation via second-instance handoff: `148.2 MB WS / 125.7 MB PM`
- Fresh repo-local debug re-check (new PID, cold hidden launch after 30s):
  - hidden steady state: `63.7 MB WS / 124.7 MB PM`
  - `keynova start` control-plane handoff: `10.2 ms`, with post-wake samples around `104.8-105.7 MB WS / 124.7-124.8 MB PM`
  - later warm hide via `Ctrl+K`: about `90.8 MB WS / 124.5 MB PM`
- Direct `cargo build --release` binary re-check is mixed:
  - cold hidden sample hit `337.7 MB WS / 124.8 MB PM`
  - warm active sample after `keynova start`: `334.7 MB WS / 129.1 MB PM`
  - warm hidden sample after `Ctrl+K`: `273.6 MB WS / 126.3 MB PM`
  - PM stayed aligned with the debug reading, but WS did not, so the release binary is not yet a safe replacement baseline for `PERF.1.FU`.

## COMPLETED: BRAND.ICON - brand icon + wordmark shipped on main

Brand commits `834ab1f` (icon swap across desktop/tray) and `3a5ae49` (drop in-panel
wordmark, shrink logo to bottom-right) are on `main` and in tag `v0.3.0`. The
`feature/brand-icon` branch is now fully contained in `main`. Closed in `active.md`.

## COMPLETED: PREFLIGHT - startup preflight snapshot shipped on main

Commit `8c1a43b` (startup preflight snapshot + once-per-boot refresh) is on `main`
and in v0.3.0. `/model_download` no longer pays the cold path / flash-crashes on
first open. Closed in `active.md`.

## COMPLETED: REF.7.A/.B - flag gate + guard/bench scripts shipped in v0.3.0

REF.7.A (`ai.legacy_agent` runtime flag default-off + legacy mount gate, `063a30c`)
and REF.7.B (`scripts/code-guard-size.mjs` + `scripts/bench-ai-capability.mjs`,
`5b21995`) are merged to `main` and released as tag `v0.3.0` (`b3d848b`).

## Detailed Notes - REF.7 close-out / doc reconciliation

- Found the docs badly out of sync with git: `active.md` / `current.md` still
  described an in-progress `feature/ref-7-quantitative-gates` branch and a
  `BRAND.ICON [~]`, but `main` already contained REF.7.A/.B, BRAND.ICON, PREFLIGHT,
  and the tagged `v0.3.0` release. `dev` is strictly behind `main` (no dev-only
  commits).
- Branch policy note: forked the close-out branch `feature/ref-7-closeout` from
  `main` rather than `dev`, because REF.7/v0.3.0 do not exist on `dev` and forking
  from `dev` would drop shipped work. Did not merge `main` into `dev` (repo policy
  forbids it). No merges performed; branch left for user review.
- Committed the previously-staged `README.md` v0.3.0 edits (matched against the
  already-shipped `docs/release-notes/v0.3.0.md`).
- Reconciled `active.md`, `current.md`, and the REF.7.C section of
  `refactor-ai-capability.md` to the shipped reality. Recorded the documented
  deviation that REF.7.C release notes shipped as version-named `v0.3.0.md` instead
  of the originally specified `REF.7-ai-interaction-model.md`.
- Scaffolded ADR-0029 section 10 `Measurement (REF.7)` with an empty target/reading
  table, every cell `pending REF.7.D`. ADR stays `accepted` (data fill, not a
  decision change).

## Open handoffs

- **REF.7.D** (user-action): `ollama pull qwen2.5:7b && npm run bench:ai -- --runs 10 --model qwen2.5:7b`. Only `qwen3:0.6b` is local.
- **REF.7.C** closes by pasting the REF.7.D output into ADR-0029 section 10 + user-side Bug A/B smoke.
- **PERF.1.FU** still needs release-path follow-up: debug `tauri-app.exe` is re-validated, but the direct release binary keeps an unexpectedly high working set even after warm hide.
- **REF.8** stays gated on the observation window (1 minor release tag + 14 days, no P0 regression).

## COMPLETED: UI /note color tidy

Replaced the four raw Tailwind colors in `NoteEditor.tsx` (`text-white` active item
+ Create hover, delete `Yes` `red-300`, delete `No` `gray-300`) with `--kn-*` tokens,
and added `--kn-danger-strong` (mirrors `--kn-accent-strong`) so the destructive
`Yes` hover has a real brighter-danger token. The cancel `No` button is now neutral
by default instead of alarming red until hovered. `npm run lint` green.

## COMPLETED: /setting practical-value pass

User-directed (overrides the pre-REF.7 feature freeze). Three frontend-only
enhancements so the settings panel is usable, not just editable:

- Enum settings with schema `options` (`ai.provider`, `search.backend`,
  `agent.web_search_provider`, `translation.provider`, ...) now render a native
  `<select>` instead of a free-text input. Current value is injected if missing.
- Fields differing from `default_value` show a modified dot + inline reset; reset
  reuses `setting.set(key, default)` (no new backend route). Secrets excluded.
- A filter box searches key/label across all sections, tagging each row's section.

Refactor: extracted `SettingRow.tsx` + shared `settingTypes.ts` (adds the
previously-unwired `default_value` field) so `SettingPanel.tsx` stays under its
size-guard ceiling (16.3 KB -> 13.3 KB). Keyboard nav model unchanged. Added
`SettingRow.test.tsx` (5 tests). `npm run test` 162 pass, lint + guard:size green.
Pending: manual `tauri dev` smoke (settings IPC needs `__TAURI_INTERNALS__`).

## Next Step

- Investigate the release-only WS inflation before using any release binary reading as the `PERF.1.FU` closure baseline.
- Keep the user-visible wake-up feel / Bug A/B smoke on the handoff list alongside `REF.7.D`.
