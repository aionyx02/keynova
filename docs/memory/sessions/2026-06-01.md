---
type: session_log
status: active
priority: p2
context_policy: on_demand
owner: project
updated: 2026-06-01
---

# Session 2026-06-01

## Highlights

- Developer directed completing the remaining codeable P0 work as REF.8, with an
  explicit scope: delete the chat-first `AiPanel`, but retain the agent backend
  for possible future reuse. This lifts the REF.8 observation gate by direct
  developer instruction (CLAUDE.md conflict-resolution rule #1).
- Opened `feature/ref-8-aipanel-removal` from `main` (REF.7 / v0.3.0 lives on
  `main`, not `dev`, so `main` is the correct base — same lineage choice as
  `feature/ref-7-closeout`).
- Reused the code cut from the pre-existing, divergent `feature/ref-8-safe-cleanup`
  commit `6319e74` via `git cherry-pick -n`, then discarded that branch's stale
  docs (and its unrelated AGPL / Google-Translate / model-panel commits stayed out
  entirely) and re-authored the docs for this branch.

## COMPLETED: REF.8 (partial) - delete chat-first AiPanel UI, retain agent backend

Frontend chat surface removed; backend agent retained dormant per developer
decision.

Deleted:
- `src/components/AiPanel.tsx` (979 lines).
- `ai_legacy` route + `AiPanelLazy` import in `src/components/panel/PanelRegistry.tsx`.
- `AiLegacyChatCommand` (`ai_legacy_chat` builtin) in `handlers/builtin_cmd.rs` and
  its flag-gated boot registration in `src-tauri/src/app/state.rs`.
- `ai.legacy_agent` entry from `useLauncherSettings.WATCHED_KEYS`.
- `AiPanel.tsx` ceiling row in `scripts/code-guard-size.mjs`.

Retained (developer decision — dormant asset for possible future tool-using
capabilities):
- `core/agent_runtime.rs` + `handlers/agent/*` untouched as runtime.
- `ai.legacy_agent` config flag in `settings_schema.rs`, re-described as a reserved
  backend gate with no UI ("Legacy agent backend (reserved, no UI)").
- `handlers/agent/mod.rs` still honours the flag for the backend run path; its
  disabled-state message + comment were rewritten to stop promising a panel that
  no longer exists.
- `src-tauri/src/models/agent.rs` comment updated (no more `AiPanel` consumer
  reference).

Not done (open, no longer gated but deferred as a product decision):
- Backend agent trim/removal, `ai.legacy_agent` flag removal, and marking
  superseded ADRs 0011 / 0016 / 0022 / 0026.

## Detailed Notes

Verification on `feature/ref-8-aipanel-removal`:
- `cargo clippy --lib -- -D warnings`: clean.
- `cargo test --lib`: 411 passed, 2 ignored.
- `npm run lint`: clean. `npm run guard:size`: clean.
- `npm run build`: clean; the AiPanel chunk no longer emits (`dist/assets` has no
  `AiPanel-*.js`).
- `npm run test`: 162 passed, 1 skipped, 1 failed. The single failure is
  `src/features/settings/SettingPanel.test.tsx`, a pre-existing untracked WIP file
  (ai.api_key secret-redaction, paired with the untracked
  `docs/adr/0041-secret-at-rest-keychain.md`). It is unrelated to REF.8 and was not
  touched; flagged to the developer.

Branch note: the older `feature/ref-8-safe-cleanup` branch already carried this
exact deletion in `6319e74`, but bundled unrelated work (AGPL relicense, Google
Cloud Translation v2, three-`/model_*`-into-one-tab merge, settings redaction) on a
pre-v0.3.0 base. A fresh branch off `main` was used to keep REF.8 isolated and
mergeable; that branch's docs/session file (`2026-05-31.md`) was dropped during the
cherry-pick.

## Fixed: v0.3.0 Linux + macOS release CI failures

Diagnosed from the user-supplied failing release-workflow logs (the `v*` tag run).

Linux (`ubuntu-22.04`) — apt `exit code 100`, held broken packages:
- Root cause: `.github/workflows/release.yml` installed BOTH `libappindicator3-dev`
  AND `libayatana-appindicator3-dev`; the ayatana `-dev` package `Conflicts:`
  the old `libappindicator3-dev`, so apt refused to resolve.
- Fix: dropped `libappindicator3-dev` from the install list. Tauri v2 uses the
  ayatana appindicator; keeping only `libayatana-appindicator3-dev` is the
  current correct set.

macOS (`universal-apple-darwin`) — compile succeeded, then
`failed to bundle project Failed to copy binary from ".../release/keynova": does not exist`:
- Root cause: the crate has two bins — `tauri-app` (GUI, `src/main.rs`, the
  `default-run`) and `keynova` (control-plane CLI, `src/bin/keynova.rs`). The
  macOS bundler resolves the main app binary by case-insensitively matching the
  `productName` ("Keynova" → "keynova") against bin targets, so it picked the
  `keynova` CLI bin and looked for `release/keynova`. But the universal build only
  lipo's the app binary (`default-run` = `tauri-app`) → `release/tauri-app`
  exists, `release/keynova` does not. Windows is case-sensitive in this match, so
  it fell back to `tauri-app` and worked — hence Windows-only success.
- Fix: set `"mainBinaryName": "Keynova"` in `tauri.conf.json`. This explicitly
  overrides the app binary name (source = the `default-run` cargo output
  `tauri-app`, output = `Keynova`), bypassing the productName-vs-bin-name
  collision. It matches the inner-exe name Windows already produces, so no Windows
  regression, and does not rename the user-facing `keynova` CLI. The CLI's
  `gui_candidates` already lists `"Keynova"`, so `keynova start` still finds the
  bundled GUI.

Verification limit: the Linux apt fix is logically confirmed against the
conflict; the macOS `mainBinaryName` config is schema-valid (CLI 2.11.0) and JSON
parses, but the bundle itself can only be confirmed by the next macOS CI run — no
local macOS/Linux build is possible from this Windows host.

## Release: v0.4.0

Version bumped 0.3.0 → 0.4.0 across `package.json`, `src-tauri/tauri.conf.json`,
`src-tauri/Cargo.toml`, `src-tauri/Cargo.lock` (minor bump: REF.8 removed the
legacy chat UI, and this is the first build where Linux/macOS actually ship — the
prior v0.3.0 Linux/macOS jobs failed, so only Windows shipped).

README refreshed for stale content: `v0.4.0` highlights section, `AiPanel` / chat
UI marked removed (was "相容模式（可用）"), `ai.legacy_agent` re-described as a
reserved no-UI backend flag, REF.7 WS investigation marked resolved (~80 MB), and
REF.8 marked partial-done.

Code signing: requested by the developer so downloads are not blocked. Not done in
this release — macOS notarization needs a paid Apple Developer ID + notarization
credentials, and Windows needs a purchased Authenticode cert; neither can be
obtained from this host. The CI can be wired to consume those as GitHub Secrets
once the developer provides them. Tag/release-trigger held pending that decision.
Decision: ship v0.4.0 unsigned now (dev choice) — tagged `v0.4.0` and pushed,
release CI triggered.

## Branch consolidation into main

Audit: only 3 of ~25 branches had commits not already in main —
`fix/icon-warm`, `feature/brand-icon`, `feature/ref-8-safe-cleanup`. The other 22
(incl. `dev`) are fully merged and are deletion candidates.

Per developer decision, bringing all three in, preserving every feature, and
relicensing to AGPL-3.0-or-later:
- `fix/icon-warm` (1 commit): icon disk-cache pre-warm to kill first-render jank.
  Merged `--no-ff`; only doc conflicts (stale branch copies of session/active +
  a stale `BUG.ICON-WARM` marker in `current.md`) — resolved to main's current
  state, code auto-merged.
- `feature/brand-icon` (5 commits): Keynova brand icon + palette/panel styling.
- `feature/ref-8-safe-cleanup`: cherry-pick only the feature commits (Google
  Cloud Translation v2, sensitive-setting redaction, translation/preview
  hardening, `/model_*` → single tabbed panel, AGPL relicense); SKIP its two
  REF.8 commits (`db23c0b`, `6319e74`) — main already did REF.8 differently.

### Outcome

- `fix/icon-warm`: merged (`9c1fb40`).
- `feature/brand-icon`: **NOT merged.** It forks from `92a3d0d` (REF.3 era, pre
  REF.6.H feature-first migration), so it edits panels at old `src/components/*`
  paths that main has moved to `src/features/*` → 6 modify/delete + content
  conflicts that cannot be auto-merged without reverting REF.6.H. Critically, its
  headline asset — the Keynova brand icons — is ALREADY on main (BRAND.ICON shipped
  in v0.3.0; `git diff main feature/brand-icon -- src-tauri/icons` is empty). The
  only unique remainder is a broad styling refresh on pre-migration paths; that
  should be re-ported as a fresh reviewed change, not force-merged into released
  main. Skipped pending developer call.
- `feature/ref-8-safe-cleanup` feature commits cherry-picked (`-x`, hooks disabled
  during pick to avoid the per-commit docs-guard) in order, every code hunk
  auto-merged — the only conflicts were the stale `2026-05-31.md` session file
  (removed) and `2026-06-01.md` / `active.md` (kept main's): `35f6d37` Google
  Cloud Translation v2, `31726bf` translation/preview harden, `b601dda` setting
  redaction, `fc04a8d` AGPL relicense (LICENSE → AGPL-3.0, `license` field in
  package.json + Cargo.toml, `docs/license-compliance.md` added; README kept
  main's and rewritten separately), `536502c` `/model_*` → single tabbed
  `ModelPanel`, `a50bf0e` model tab scroll fix.

Verification after all cherry-picks: `cargo clippy --lib -- -D warnings` clean;
`cargo test --lib` 424 passed; `npm run lint` / `guard:size` / `build` clean
(`ModelPanel` chunk emits, old three model chunks gone); `npm run test` 162 passed
(only the pre-existing untracked `SettingPanel.test.tsx` WIP fails — its frontend
field assertion is not satisfied by the backend-only redaction commit).

## Fixed: v0.4.0 Windows MSI bundle failure

The v0.4.0 tag CI got further on Windows than before — `mainBinaryName` worked
(built `release\Keynova.exe`, WiX `candle` compiled `main.wxs`) — but WiX `light.exe`
(the MSI linker) failed with no surfaced error, and an MSI failure exits the whole
Windows job (so NSIS artifacts don't upload either). Suspected cause: on Windows'
case-insensitive FS the renamed GUI `Keynova.exe` collides with the `keynova.exe`
control-plane CLI bin during the WiX harvest, but `light.exe`'s real error was not
in the log.

Fix: drop MSI from `bundle.targets` (`"all"` → `["nsis","app","dmg","deb","appimage"]`),
shipping NSIS as the sole Windows installer (already the documented primary; WiX is
not involved in NSIS). README Windows download row updated to NSIS-only. MSI can be
revisited later if enterprise GPO deployment is needed.

NOTE: the `v0.4.0` tag (`479ebf8`) predates the branch-consolidation cherry-picks
AND this MSI fix, and its Windows job failed on MSI — so v0.4.0 shipped no usable
artifacts. The accumulated work (AGPL relicense, Google Translate v2, single-tab
ModelPanel, redaction, icon-warm, MSI fix) needs a fresh tag + version bump (propose
v0.5.0) to actually ship.

## README full rewrite

Rewrote `README.md` to drop the P0-refactor framing (per developer: P0 refactor is
done) — removed the REF roadmap table + quantitative-gates section. Now product-
first: feature table, download/install, usage, inline-AI, build, config,
architecture, a concise "計畫中" list (signing, auto-updater, secret keychain,
deeper OS integration), platform support, memory footprint. Reflects current
reality: AGPL-3.0 license badge + section, Google Cloud Translation v2, single
tabbed `/model` panel, setting redaction, legacy chat removed.