---
type: session_log
status: active
updated: 2026-06-14
owner: project
---

# Session 2026-06-14

## COMPLETED: XPLAT - cross-platform stability Phase 1 + v0.7.1 release cut

### Scope

Consolidated `feature/cross-platform-stability` and cut **v0.7.1** — a
stability/perf/CI point release bundling everything unreleased since the
`v0.7.0` tag (`c513416`).

### Version decision

`v0.7.1` (patch). The delta is fixes + perf + CI hardening only — no new
features, no breaking changes. Bumped four locations to keep them consistent
(`release-notes.mjs` asserts package.json === tauri.conf.json):
`package.json`, `src-tauri/tauri.conf.json`, `src-tauri/Cargo.toml`,
`src-tauri/Cargo.lock` (`tauri-app` entry).

### Delta bundled (v0.7.0 → v0.7.1)

main, post-v0.7.0:
- `d5460df` fix(windows): suppress black console flash on subprocess spawns
- `6518c73` perf(windows): native shell-icon extraction, drop per-icon PowerShell
- `7a6c179` perf(ai): memoize hardware probe, drop repeated wmic/nvidia-smi spawns
- `88c78ad` docs(tasks): record PERF.REOPEN/STAB.4 dispositions

feature/cross-platform-stability:
- `a38a736` ci: add per-push clippy+test matrix for ubuntu/macos/windows
- `fa88ff8` docs(ci): record cross-platform CI verified green on all 3 OSes

### Release mechanics

`release.yml` triggers on `v*` tag push: `npm run verify` → 3-OS tauri build →
**draft** release (`releaseDraft: true`, unsigned). Release notes loaded from
`docs/release-notes/v0.7.1.md` by `scripts/release-notes.mjs` (body must start
`# Keynova v0.7.1`). Process: local `verify` green → commit bump+notes on
feature branch → `--no-ff` merge to `main` → tag `v0.7.1` → push main + tag →
watch workflow → developer manually publishes the draft. Certs still deferred
per developer (stays unsigned).

### Cross-platform Phase 1 status

Phase 1 (CI gate + static review) complete and verified green on all three OSes
(see `tasks/cross-platform-stability.md`). Phase 2 (fill empty
`platform/linux.rs` / `platform/macos.rs` feature skeletons) deferred behind the
now-green Phase-1 CI gate.
