---
type: session_log
status: active
priority: p2
updated: 2026-05-23
context_policy: on_demand
owner: project
---

# Session 2026-05-23

## Highlights

- REF.4 landed: stateless AI capability layer (`core/ai_capability/`) with three initial capabilities (`explain`, `summarize`, `fix_error`), IPC namespace `capability.*`, and frontend `useCapability` hook.
- No UI mount this batch — palette wiring waits for REF.6 per agreed scope. Clean boundary, no `CommandPalette.tsx` edits.
- 19 backend unit tests + 5 frontend hook tests added; cargo clippy `-D warnings` and npm lint clean.

## Detailed Notes

### Backend module shape

- `core/ai_capability/mod.rs` — single public entry `call_capability(req, deps)`; compile-time `match` on `CapabilityId` (enum-based registry, no `dyn Capability`).
- `core/ai_capability/registry.rs` — `CapabilityId::{Explain,Summarize,FixError}` + static `CapabilityMeta` table; `meta(id).audit` and `accepts_context_hash` are fixed at registration per ADR-0030 §4, never returned per-call.
- `core/ai_capability/contract.rs` — `CapabilityRequest`, `CapabilityResponse`, `CapabilityOutput::{Text,Structured}`, `CapabilityError::{UnknownId,InvalidPayload,ProviderError,Cancelled,UnsupportedAction}`. `ChatProvider` trait abstracts the LLM call so unit tests can stub; production impl is `AiManagerChatProvider` wrapping `AiManager::chat_sync` / `chat_sync_stream`. `StreamChunkFn` alias keeps clippy `type_complexity` happy.
- `core/ai_capability/prompt.rs` — `CAPABILITY_PROMPT_BUDGET_CHARS = 1400`, `CAPABILITY_PROMPT_SOURCE_LIMIT = 6`, `CAPABILITY_SNIPPET_MAX_CHARS = 160`. Budget overrun drops the context block first, then truncates the task body via `core::grounding::truncate` (char-boundary safe). `maybe_audit` writes through `KnowledgeStoreHandle::try_log_agent_audit` only when `audit_required == true`.

### Capabilities

- `explain` — pulls workspace/commands/notes/history sources via `LocalContextSearcher`, best-effort (errors non-fatal). `audit = true`, `risk_tag = none()`.
- `summarize` — pure text transform, no local grounding. `audit = false`, `risk_tag = none()`.
- `fix_error` — accepts either `{raw_output}` (preferred, no exec) or `{program,args,cwd,timeout_secs}` which re-runs via `core::dev_runner::run_bounded_dev_cmd`. Program allowlist: `cargo`, `npm`, `pnpm`, `yarn`, `tsc`. Any `{apply:...}` variant returns `UnsupportedAction` (file-mutating fix path deferred to a future ADR). `audit = true`, `risk_tag = none()`.

### Handler + wiring

- `handlers/ai_capability.rs` — `capability.list` / `capability.call` / `capability.cancel`. Worker is `std::thread::spawn`; per-`request_id` `Arc<AtomicBool>` cancel flag in a `HashMap`. No global single-flight — distinct request ids may run in parallel; per-chip UI disable owns serialization (ADR-0029 §4 state-based). Streaming emits `capability.stream.chunk` events; terminal event is `capability.response`.
- `core/local_context.rs` — added `#[derive(Clone)]` so the handler can clone the searcher into each spawned thread cheaply (all fields are already `Arc`).
- `managers/ai_manager.rs` — added `chat_sync` + `chat_sync_stream` blocking shims (single-user-turn, no history mutation; reuse existing `do_chat` / `do_chat_stream`).
- `app/state.rs` — registered `AiCapabilityHandler` with `LocalContextSearcher` constructed from the same managers the agent handler uses, plus `KnowledgeStoreHandle` and the event bus.
- `core/mod.rs`, `handlers/mod.rs` — exposed the new modules.

### Frontend feature

- `src/features/ai-capability/types.ts` — TS twin (`CapabilityId`, `CapabilityMeta`, `CapabilityOutput`, request/response event payloads, per-capability payload types). Re-uses `RiskTag` and `parseRiskTagFailSafe` from `src/types/unified-result.ts` — no duplication.
- `src/features/ai-capability/hooks/useCapability.ts` — generic single-call hook. State-based `isLoading` for ADR-0029 §4 in-flight disable. Subscribes to `capability.response` (filtered by `request_id`) + optionally `capability.stream.chunk`. Outside Tauri (`window.__TAURI_INTERNALS__` absent) it short-circuits listener setup, matching the jsdom pattern in `useSearchBackend`.
- `src/ipc/routes.ts` — added `CAPABILITY_LIST` / `CAPABILITY_CALL` / `CAPABILITY_CANCEL`.

### Live integration test (gated)

- New cargo feature `live-ai` (default off) in `src-tauri/Cargo.toml`.
- `core/ai_capability/live_tests.rs` (cfg-gated, also `#[ignore]`) — two tests against `localhost:11434` for `explain` and `fix_error`. Model defaults to ADR-0029's `qwen2.5:7b`; override via `KEYNOVA_LIVE_AI_MODEL` env var. Prints elapsed-ms latency to stdout. Run with `cargo test --features live-ai -- --ignored ai_capability_live --nocapture`. Latency vs the 800 ms P50 target is recorded here, not asserted (REF.7 owns quantitative gates).

### Live Ollama smoke (qwen2.5:0.5b, cold-start single sample)

Ran against the only model currently installed locally (`qwen2.5:0.5b`, ~400 MB) to validate the end-to-end pipeline. `qwen2.5:7b` was not pulled this session; the canonical ADR-0029 §8 reading remains pending.

| Capability                    | Latency | Result                                               |
| ----------------------------- | ------- | ---------------------------------------------------- |
| `explain`                     | 4650 ms | text reply, `risk_tag.requires_confirmation = false` |
| `fix_error` (raw cargo E0308) | 4998 ms | text reply, `risk_tag.requires_confirmation = false` |

Interpretation:

- End-to-end pipeline (`capability.call` → `AiManagerChatProvider::chat` → `do_chat` → Ollama HTTP → response) works against a real daemon. No code changes needed after the smoke.
- Both samples include cold-load (Ollama keep_alive default reset between runs); single-sample N=1 each, not a P50.
- Numbers are nowhere near the 800 ms P50 budget but **not informative for the ADR target** — a 0.5b model on CPU at ~20 tok/s easily takes 3–4 s to produce 60+ output tokens regardless of model load. The 7b reading must be retaken on hardware that actually serves it (GPU / Metal) before any conclusion.

Pending follow-up for the next session: `ollama pull qwen2.5:7b` then re-run the live tests warm (consecutive calls with `keep_alive: "5m"`) to capture a real P50/P95 against the ADR target. Note any gap inline here for REF.7.

## COMPLETED: REF.5 - Workflow memory (schema v4 + record/suggest, backend + hooks only)

Workflow memory lands as the second backend-only batch (no `CommandPalette.tsx` mount, REF.6 will wire the surface). 11 backend tests + 5 frontend hook tests green; clippy `-D warnings` + npm lint clean.

### Schema v4

- Bumped `CURRENT_SCHEMA_VERSION` 3 → 4 in `src-tauri/src/core/knowledge_store.rs`.
- Appended `workflow_history` DDL + two indexes to `init_schema()` (`idx_workflow_history_context (context_hash, executed_at DESC)`, `idx_workflow_history_executed (executed_at DESC)`).
- Migration uses the existing auto-backup path: on next open of an existing v3 DB, `open_connection()` creates `…-v3-to-v4-{uuid}.db` under the store's `backups/` dir, runs the idempotent DDL, and records the v4 row in `schema_migrations`. **No down-migration code exists** — rollback guidance per ADR-0029 §4 is "open the auto-backup, or `DROP TABLE workflow_history;` + `PRAGMA user_version = 3`." v3 tables are untouched (purely additive).

### Module split

- `src-tauri/src/core/knowledge_store.rs` — added `WorkflowHistoryEntry`, `WorkflowHistoryRow`, `DbRequest::{WriteWorkflowHistory, ReadRecentWorkflows}`, `KnowledgeStoreHandle::{try_log_workflow_history, recent_workflows, recent_workflows_blocking}`, `insert_workflow_history` + `read_recent_workflows` SQL. Mirrors the agent-audit pattern.
- `src-tauri/src/core/workflow_memory.rs` — `compute_context_hash(workspace_id, mode, panel)` (DefaultHasher; coarse on purpose), `digest_payload(payload)` (deterministic via canonical serde), `record`, `suggest`, `clamp_limit`. `DEFAULT_LIMIT = 5`, `MAX_LIMIT = 50`.
- `src-tauri/src/handlers/workflow_memory.rs` — `workflow.recent` + `workflow.suggest`. Suggest resolves `context_hash` server-side from `WorkspaceManager::current()` so frontend never has to know the hash formula.

### Record hook

- `cmd_dispatch_impl` Ok branch in `src-tauri/src/app/dispatch.rs` — central hook for routes `cmd.run` (label = `payload.name`) and `capability.call` (label = `payload.id`). Payload is cloned cheaply only when route is on the allowlist.
- `run_action_command` Ok branch — second hook for `action.run` where the resolved `action.label` is at hand (avoids double action_arena resolution).
- `record_workflow_event` helper swallows workspace-lock failures: workflow recording is best-effort and must never block the critical IPC path. Lock failure → entry recorded without `context_hash` + `workspace_id`.

### Frontend

- `src/features/workflow-memory/types.ts` — `WorkflowHistoryRow` + `WorkflowSuggestResponse`.
- `src/features/workflow-memory/hooks/useWorkflowMemory.ts` — `useRecentWorkflows()` + `useSuggestedWorkflows()` produced by a `makeWorkflowQueryHook(route)` factory (renamed from `useWorkflowQuery` to avoid ESLint rules-of-hooks false-positive on the factory). Outside Tauri the hooks short-circuit to empty rows (same jsdom pattern as `useSearchBackend` + REF.4 `useCapability`).
- `src/ipc/routes.ts` — `WORKFLOW_RECENT` + `WORKFLOW_SUGGEST`.

### Tests

- Backend: 11 unit tests in `core/workflow_memory` (context_hash determinism + uniqueness, digest stability across reserialization, clamp limit bounds, record/suggest roundtrip with `context_hash` filter, recency ordering) + 3 handler payload-parse tests.
- Frontend: 5 Vitest cases — jsdom short-circuit, IPC dispatch happy path, error surfacing, `refresh()` re-issues the call, suggest vs recent route selection. The `withTauriShim` helper temporarily sets `window.__TAURI_INTERNALS__` and `await`s the async body (initial sync version cleared the shim before the test ran).

### Non-goals (per plan)

- No palette mount — REF.6 wires it.
- No record on `setting.*`, `search.query`, `search.record_selection`, `file.preview`, etc.; only the three allowlist routes.
- No `suggest_next` capability (waits for capability extension), no embeddings, no retention/pruning, no telemetry.

### Migration smoke not run

Auto-backup on first open of an existing v3 DB was **not** physically smoked this session — verified only via the schema-version test path. The v3 → v4 migration is exercised the next time the user runs `npm run tauri dev` against their existing store; the backup file will appear at `%APPDATA%/com.tauri.app/keynova-data/backups/keynova-v3-to-v4-{uuid}.db`.

## COMPLETED: REF.6.A - Inline AI MVP (UnifiedResult + ActionChip + explain)

User-visible inline AI surface lands. `search.query` wire format is now `UnifiedResult[]`, `SearchResultsList` renders an Explain `ActionChip` on every row, `Ctrl+E` triggers `useCapability("explain")` against the focused result, and `InlineCapabilityReply` streams the response below the list. Bug A focus race and Bug B kill-set paths verified green by unit tests (manual smoke pending live Ollama warm-up).

### Backend (wire format)

- `src-tauri/src/models/unified_result.rs` — added `SourceMetadata.secondary_action_count: Option<u32>` (additive per ADR-0030 §4) so the legacy "+N" badge stays renderable without polluting `actions[]`. `From<UiSearchItem>` populates it.
- `src-tauri/src/handlers/search.rs` — new `to_unified_results(items)` helper wraps the existing `.into()` shim. Conversion happens at the three IPC emit boundaries: sync return (`execute_sync_query`), stream-init batch (`execute_stream_query`), and chunk publish (`emit_search_chunk`). `UiSearchItem` stays internal to the worker.
- All 48 `search` lib tests + 10 `unified_result` tests pass; one new `ui_search_item_shim_populates_secondary_action_count` test covers the new field.

### Frontend (data flow)

- `src/types/search.ts` — `SearchChunkPayload.items` is `UnifiedResult[]`; legacy `SearchResult` gains `unified_id` for bridging.
- `src/types/unified-result.ts` — mirrored optional `secondary_action_count` field.
- `src/utils/search.ts` — sort/quota/merge helpers operate on `UnifiedResult` (`sortUnifiedResults`, `mergeUnifiedResults`, `unifiedResultKey`). `unifiedToLegacy(u)` adapter feeds the unchanged downstream hooks.
- `src/features/command-palette/hooks/useSearchStream.ts` — canonical type is `UnifiedResult[]`; test fixtures updated.
- `src/components/CommandPalette.tsx` — `useMemo` derives `legacyResults` once and feeds `useSearchMetadata` / `useDerivedView`; computes parallel `visibleUnified` for the chip column. Hoists `useCapability({ id: "explain" })` + `handleExplain` + `explainOpen` state. New mount: `<InlineCapabilityReply>` between the result list and `<RankTooltip>`.
- `useFileActions` / `useTerminalControl` — `setResults` prop type widened to `Dispatch<SetStateAction<UnifiedResult[]>>`; internal logic still operates on `SearchResult` via `legacyResults` flowing in from CommandPalette (per `feedback-task-persistence`'s drop-list rule, full migration of these hooks is left for REF.6.B).

### UI

- `src/features/command-palette/SearchResultsList.tsx` — accepts both `visibleResults: SearchResult[]` (legacy row shape) and `unifiedVisible: UnifiedResult[]` (canonical, indexed-parallel). New rightmost element on every row: an "Explain" `<button>` chip styled like `FilterChips`. `onMouseDown` + `e.preventDefault() + e.stopPropagation()` keeps focus on the palette input and prevents the row's `onLaunch` from firing. Chip is disabled (label collapses to "…") when `explainLoading && i === safeSelected`.
- `src/features/command-palette/InlineCapabilityReply.tsx` (new) — pure presentational stream panel: "Streaming…" placeholder, accumulated text with `whitespace-pre-wrap`, red error line, Cancel/Close button bound to `onCancel`.
- `src/features/command-palette/hooks/useKeyboardNav.ts` — Ctrl+E (also Meta+E for macOS) branch at the top of search-mode handling. Only fires when a result is focused and `!explainLoading`. Routes through `onExplain(focusedResult.title || focusedResult.name)`.

### Tests

- New `SearchResultsList.test.tsx` (4 tests): chip renders, click calls `onExplain` with title, disabled state when loading + focused, chip click doesn't fire `onLaunch`.
- New `InlineCapabilityReply.test.tsx` (5 tests): streaming placeholder, accumulated text, error rendering, Cancel/Close button.
- Updated `useSearchStream.test.ts` (9 tests, all green) to UnifiedResult shape and `rank.score` assertion.
- `vitest.setup.ts` — added a no-op `Element.prototype.scrollIntoView` polyfill so `SearchResultsList` row refs don't crash under jsdom.
- Full frontend suite: 63/63 across 13 files. `npm run lint` clean. `cargo clippy --lib -- -D warnings` clean. `cargo test --lib` 390 pass / 1 pre-existing nvim environmental fail (confirmed unrelated).

### Non-goals honoured (per the locked scope)

- No `CommandPalette.tsx` line-count push (simplification-only goal dropped). Current line count is higher than pre-REF.6.A because of the new explain plumbing; that's accepted.
- No removal of `AiPanel` / `TerminalPanel` mount — REF.6.B.
- No `useFileActions` / `useDerivedView` / `useKeyboardNav` / `useSearchMetadata` / `useFilePreview` / `PreviewPane` / `secondaryActions.ts` migration to UnifiedResult — they still consume the legacy `SearchResult` shape via the `unifiedToLegacy` adapter. REF.6.B/C will revisit.
- No `cmd.run` / `BuiltinCommandResult` row migration; `CommandResultArea.tsx` untouched.
- No new ADR.

### Pending manual verification

- `npm run tauri dev` smoke: type a query → Explain chip appears on rows → Ctrl+E streams reply → Esc cancels. Bug A focus race + Bug B kill-set manual regression. Pending the next live session; the unit-test coverage above pins the key invariants.

### Live-AI streaming smoke (added post-commit)

Added `ai_capability_live_explain_streams_chunks` (cfg(feature="live-ai"), `#[ignore]`) to exercise the **streaming** path against a real Ollama daemon — the existing live tests only covered non-streaming `chat()`. Captures every `stream_chunk` callback into a `Vec<String>` and asserts (a) ≥1 chunk fires, (b) concatenated chunks == final reply text, (c) prints latency for REF.7 P50/P95 tracking.

Verified locally with `KEYNOVA_LIVE_AI_MODEL=qwen2.5:0.5b`: 376 chunks, 1534-char accumulated reply, 1852 ms (warm). This conclusively proves the chip + Ctrl+E end-to-end backend path works; any "Streaming…" hang in `tauri dev` is purely a config mismatch (`ai.model` defaults to `qwen2.5:7b` which may not be pulled).

### Checks

- `cargo build --lib` ✓
- `cargo clippy --lib -- -D warnings` ✓ (after type-alias for `Arc<dyn Fn(&str) + Send + Sync>`, rename `CapabilityId::from_str` → `parse` to avoid `should_implement_trait`, and `allow(clippy::too_many_arguments)` on `chat_sync_stream`).
- `cargo test --lib ai_capability` ✓ (19/19).
- `cargo test --features live-ai --no-run` ✓ (live test compiles).
- `cargo test --lib` ✓ 378 pass; 1 environmental pre-existing failure (`note_lazyvim_missing_nvim_returns_inline_guidance` fails because `nvim` is installed on this dev machine — confirmed unrelated to REF.4 via `git stash` reproduction).
- `npm run test` ✓ 49/49 (10 files including the new `useCapability.test.ts` with 5 tests).
- `npm run lint` ✓ clean.

## COMPLETED: REF.4 - Stateless AI capability layer

Initial three capabilities (`explain`, `summarize`, `fix_error`) shipping behind `capability.*` IPC + `useCapability` hook. No palette mount — that lands in REF.6. `agent_runtime.rs` untouched; `ai.legacy_agent` default stays `true`. Compile-time enum-match registry per agreed scope (no `dyn Capability`). Live-Ollama latency reading deferred to next session; capability path covered by 19 stub-provider unit tests in this batch.

Next runnable batch: REF.5 (workflow memory schema v4 + `core/workflow_memory.rs`) may now start in parallel with REF.6 prep, per the active queue plan.
