---
type: session_log
status: active
priority: p2
updated: 2026-05-29
context_policy: on_demand
owner: project
---

# Session 2026-05-29

## Highlights

- Ran a repo housekeeping pass focused on project structure rather than runtime
  behavior.
- Replaced leftover Vite shell branding with a lightweight Keynova favicon and
  title in `index.html`.
- Tightened ignore rules for frontend, Tauri, and local debug artifacts so
  transient files stay out of the repo view.
- Moved loose root-level `.docx` reference/template files under `docs/` and
  updated the generator script to write into `docs/templates/`.
- Added a safe `npm run clean:local` maintenance script and removed the
  duplicate root brand image in favor of `src/assets/keynova_icon.png` as the
  single icon source.

## Detailed Notes

### Housekeeping pass

Scope:

- Keep the cleanup low-risk and non-architectural during the active REF.6
  refactor track.
- Prefer structural hygiene: ignore rules, starter leftovers, and document
  placement.

Changes:

- Normalized `.gitignore` and `src-tauri/.gitignore` comments to clear ASCII
  sections and added entries for `coverage/`, `src-tauri/target/`,
  `src-tauri/gen/schemas/`, `src-tauri/WixTools/`, and local debug traces.
- Added `public/keynova.svg` and pointed `index.html` at it so the web shell no
  longer ships Vite starter branding.
- Removed unused starter assets after confirming they were unreferenced.
- Relocated root `Prompt_Engineering_Init_Template.docx` to
  `docs/templates/Prompt_Engineering_Init_Template.docx`.
- Relocated root `ai_agent_adr_development_rules_v2.docx` to
  `docs/reference/ai_agent_adr_development_rules_v2.docx`.
- Clarified that `docs/tasks/refactor-ai-capability.md` cites an external
  planning doc (`Keynova_Refactor_Plan_v1-2.docx`) rather than a repo-root
  file that no longer exists locally.
- Updated `scripts/generate_prompt_engineering_doc.py` so future template
  exports land in `docs/templates/` and create the directory automatically.
- Added `scripts/clean-local-artifacts.mjs` plus a `clean:local` package
  script to remove `dist/`, `coverage/`, and local debug logs without touching
  dependencies or Rust build caches.
- Added `tauri:icon` to `package.json` so Tauri icon regeneration now targets
  `src/assets/keynova_icon.png`, allowing the duplicate repo-root
  `keynova_icon.png` to be removed.

Verification:

- `npm run docs:refresh`
- `npm run build`
- `npm run clean:local`
