// REF.6.A's per-row Explain chip tests removed in REF.6.B clean cut.
// SearchResultsList row rendering is exercised through the palette
// integration smoke (Bug A/B manual regression) and other unit tests.
// Keeping the file as a placeholder so future row-level tests have a home.

import { describe, it } from "vitest";

describe("SearchResultsList", () => {
  it.skip("row rendering covered by manual smoke until row-level unit tests return", () => {
    // intentional no-op
  });
});
